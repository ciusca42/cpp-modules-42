#ifndef EASYFIND_HPP
# define EASYFIND_HPP

template <typename T>
class easyfind {
	public:
		easyfind();
		~easyfind();
		easyfind(const easyfind & obj);
		easyfind & operator=(const easyfind & obj);
};

#endif